[project]
name = "scholaragent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "llama-index",
    "llama-parse",
    "python-dotenv",
    "fastapi",
    "uvicorn"
]
